version: '3'

services:
  frontend:
    image: parthmehrotra/frontend
    ports:
      - "9000:9000"
  matchmaker:
    depends_on:
      - redis
    image: parthmehrotra/matchmaker
    ports:
      - "8000:8000"
  gameserver-service:
    image: parthmehrotra/gameserver-service
    depends_on:
      - redis
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock"
    deploy:
      placement:
        constraints: [ node.role == manager ]
  payserver:
    depends_on:
      - redis
    image: parthmehrotra/payserver
    ports:
      - "7000:7000"
  redis:
    image: redis
    ports:
      - "6379:6379"
  vis:
    image: dockersamples/visualizer
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock"
    ports:
      - "8080:8080"
    deploy:
      placement:
        constraints: [ node.role == manager ]
